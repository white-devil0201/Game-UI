import 'package:flutter/material.dart';

class LeDetail extends StatelessWidget {
  final assetPath;

  LeDetail({this.assetPath});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
